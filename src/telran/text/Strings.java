package telran.text;

import java.util.Comparator;

public class Strings implements Comparator<Strings>{
	protected Strings[] strArr = new Strings[0];
	/**
	 * 
	 * @param dates array of strings containing dates in the format D/M/YYYY
	 * 
	 * @return array sorted by dates
	 * use only class String and interface Comparator, to write tests
	 */
public static String[] sortStringsasDates(String [] dates) {
	//TODO 
	
	return null;
}


	@Override
	public int compare(Strings o1, Strings o2) {
		// TODO Auto-generated method stub
		return 0;
	}
}
